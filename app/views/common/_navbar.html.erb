<nav class="navbar w-full bg-gray-100 p-4 flex justify-between items-center">
  <div>
    <a href="/" class="text-xl">任務管理系統</a><span class="px-2 text-base text-blue-700">hello, Rails :)</span>
  </div>
  <div class="space-x-1">
    <% if current_user %>
      <span class="text-blue-500" ><%= current_user.email %></span>
      <%= link_to "登出", log_out_path, data: { turbo_method: :delete }, class:"hover:text-blue-900" %>
      <% if current_user.admin? %>
        <button><%= link_to "管理頁面", admin_users_path %></button>
      <% end %>
    <% else %>
      <%= link_to "登入", log_in_path %>
      <%= link_to "註冊", sign_up_path %>
    <% end %>
  </div>
</nav>