<%= form_for(mission) do |f| %>
  <%= f.label :title, t("mission.title") %>
  <%= f.text_field :title %>
  <br>
  <%= f.label :content, t("mission.title") %>
  <%= f.text_area :content %>
  <br>
  <%= f.submit  t("button.submit")%>
<% end %>
