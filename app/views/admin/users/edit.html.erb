<%= form_for @user, url: admin_user_path(@user), method: 'put' do |f| %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <%= f.label :roles %>
  <%= f.select :roles, options_for_select([[ "user", "user"], ["admin", "admin"]]) %>
  <%= f.submit t("button.submit") %>
<% end %>